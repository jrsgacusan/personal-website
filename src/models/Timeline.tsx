import internal from 'stream';

export type Timeline = {
  id: number;
  title: string;
  location: string;
  description: string;
  buttonText: string;
  isButtonPresent: boolean;
  href: string;
  date: string;
  icon: string;
};
